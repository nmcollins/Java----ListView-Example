<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.ncollins9293.lab5.MainActivity"
    android:background="#000000"
    android:orientation="vertical" >

    <!--From the instructions:  the first will xml file will consist of a ListView
    Obviously this is the given main activity xml file, but the required
    ListView has been added here and is separate from the row layout,
    as required-->



    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Add Item"
        android:id="@+id/btnAddNew"
        android:layout_alignTop="@android:id/list"
        android:layout_alignParentStart="true"
        android:layout_alignEnd="@android:id/list" />
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="389dp"
        android:orientation="horizontal" >>
    <ListView
        android:id="@android:id/list"
        android:layout_width="match_parent"
        android:layout_height="fill_parent"
        ></ListView>
    </LinearLayout>


</LinearLayout>
